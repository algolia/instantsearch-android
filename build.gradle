group 'com.example'
version '0.0.1'

apply plugin: 'maven-publish'

allprojects {
    repositories {
        google()
        maven { url 'https://dl.bintray.com/kotlin/kotlin-eap' }
        mavenCentral()
    }
}
