package com.algolia.instantsearch.core.selectable.list

/**
 * A mode of selection: either a [Single] item can be selected at a time,
 * or [Multiple] items are allowed.
 */
public enum class SelectionMode {
    Single,
    Multiple
}