apply from: "../common.gradle"

ext {
    BASENAME = 'instantsearch-androidx-core'
}

dependencies {
    api libraries.eventbus
    api libraries.algolia

    implementation libraries.appcompatX

    implementation(libraries.json_path) {
        exclude module: 'asm'
    }
}

apply from: '../release.gradle'
